{
  "name": "interviewiq",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client:dev": "npm --prefix client run dev",
    "client:build": "npm --prefix client run build",
    "server:dev": "uvicorn app.main:app --reload --app-dir server",
    "server:start": "uvicorn app.main:app --host 0.0.0.0 --port 8000 --app-dir server"
  }
}
